public enum LocomotionState 
{
    Idle,
    Move,
   JumpStart,
   JumpEnd,
   Fall,   
   Die,
   GetHit,
   GetUp

}
public enum CombatState
{
    
    Attack01,
    Attack02,
    Defend,
    NoneState,
    DefendHit
}
