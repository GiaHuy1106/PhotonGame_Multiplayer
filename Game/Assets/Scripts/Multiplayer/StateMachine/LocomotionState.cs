public enum LocomotionState 
{
    Idle,
    Move,
   JumpStart,
   JumpEnd,
   Fall,   
   Die,
   GetHit,
   GetUp,
   DefendHit
}
public enum CombatState
{
    
    Attack01,
    Attack02,
    Defend,
    NoneState
}
